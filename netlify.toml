[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "server"
  node_bundler = "esbuild"
  external_node_modules = ["@babel/preset-typescript"]
  nodejs_compat = true
  format = "esm"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
